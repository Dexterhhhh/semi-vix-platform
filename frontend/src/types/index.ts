export type SVIXPoint = { timestamp: string; svix: number; core: number; memory: number; ai: number; calculation_quality: number; estimated: boolean; source_feed?: string | null; calculation_method?: string; market_data_quality?: string }
export type Components = { core: number; memory: number; ai: number }
export type CalculationJob = { id: number; type: string; start_date: string; end_date: string; frequency: 'daily' | 'weekly'; status: string; progress: number; result_summary?: { records_calculated?: number; estimated_records?: number; skipped_records?: number; stock_rows?: number; option_rows?: number; stage?: string }; created_at: string; started_at?: string; finished_at?: string; error_message?: string }
export type DashboardSettings = { refresh_frequency_minutes: number; intraday_refresh_seconds: number; selected_symbols: string[]; manual_component_weights?: Record<string, number>; option_cleanup_enabled: boolean; option_retention_days: number; svix_downsample_enabled: boolean; detailed_retention_days: number; maintenance_time_utc: string }
export type SystemStatus = { database: string; market_data: string; svix_engine: string; worker: string; last_calculation?: string }
export type LifecycleStatus = { option_snapshot_rows: number; option_snapshot_bytes: number | null; svix_history_rows: number; svix_history_bytes: number | null; svix_daily_rows: number; svix_daily_bytes: number | null; last_run: null | { status: string; started_at: string; finished_at?: string; option_rows_deleted: number; history_rows_aggregated: number; daily_rows_written: number } }
export type MarketStatus = { state: 'PRE_MARKET' | 'OPEN' | 'POST_CLOSE_DELAY' | 'CLOSED'; is_collection_window: boolean; session_date: string | null; market_open: string | null; market_close: string | null; collection_end: string | null; next_open: string | null; last_collection_at: string | null; last_collection_status: string | null; next_collection_at: string | null }
export type CustomIndexComponent = { symbol: string; weight_percent: number }
export type CustomIndexConfig = { id: number; version: number; name: string; enabled: boolean; missing_policy: 'STRICT' | 'RENORMALIZE'; components: CustomIndexComponent[]; updated_at: string }
export type CustomIndexPayload = Omit<CustomIndexConfig, 'id' | 'version' | 'updated_at'>
export type CustomIndexPoint = { timestamp: string; value: number; calculation_quality: number; estimated: boolean; source_feed?: string | null; version: number }
